using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour {
	private Vector2 test;
	// Use this for initialization
	void Start () {
		//test = transform.Find ("MainChar").position;
		//Debug.Log(test);
	}
	
	// Update is called once per frame
	void Update () {
		//Debug.Log(transform.Find ("MainChar").position);
		//test = transform.Find ("MainChar").position;
		//Debug.Log(test);
		//Debug.Log(GameObject.Find ("MainChar").transform.position);
	}
}
